// AchievoGate Design System - Main Theme Export
import { animationPresets, durations, easing, motionHelpers, staggerDelays } from './animations';
import colors from './colors';
import { borderRadius, iconSizes, shadows, spacing, typography } from './spacing';

const theme = {
    colors,
    spacing,
    borderRadius,
    shadows,
    iconSizes,
    typography,
    durations,
    easing,
    animationPresets,
    staggerDelays,
    motionHelpers,
};

export default theme;
export {
    animationPresets, borderRadius, colors, durations,
    easing, iconSizes, motionHelpers, shadows, spacing, staggerDelays, typography
};

